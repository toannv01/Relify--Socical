<%= form_with(model: add_friend, local: true) do |form| %>
  <% if add_friend.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(add_friend.errors.count, "error") %> prohibited this add_friend from being saved:</h2>

      <ul>
      <% add_friend.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
